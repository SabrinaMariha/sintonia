<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quando pressionado -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <gradient
                android:angle="180"
                android:startColor="#FF5328"
                android:centerColor="#E02E3D"
                android:endColor="#C20853"
                android:type="linear"
                android:useLevel="false" />
            <corners android:radius="20dp" />
        </shape>
    </item>

    <!-- Estado padrão -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="180"
                android:startColor="#FF5328"
                android:centerColor="#E02E3D"
                android:endColor="#C20853"
                android:type="linear"
                android:useLevel="false" />
            <corners android:radius="20dp" />
        </shape>
    </item>

</selector>
