<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quando pressionado -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:startColor="@color/salmao"
                android:centerColor="@color/salmao"
                android:endColor="@color/rosaEletrico"
                android:type="linear" />
            <corners android:radius="20dp" />
        </shape>
    </item>

    <!-- Estado padrão -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="0"
                android:startColor="@color/salmao"
                android:centerColor="@color/salmao"
                android:endColor="@color/rosaEletrico"
                android:type="linear" />
            <corners android:radius="20dp" />
        </shape>
    </item>

</selector>
